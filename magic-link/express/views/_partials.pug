mixin alert(content)
  div(class=`alert shadow-lg rounded-none`, class!=attributes.class)
    div
      span= content

mixin input_error(content)
  span(class="flex items-center font-medium tracking-wide text-red-500 text-xs mt-1 ml-1")= content